export type PokemonType = {
  name: string;
};

export type PokemonDetailed = {
  name: string;
  image: string;
  experience: number;
  height: number;
  weight: number;
  types: string[];
  color: string;
};
